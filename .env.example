# ========================================================================================
# Junjo Server Environment Configuration
# ========================================================================================
#
# This file contains default settings for LOCAL DEVELOPMENT.
# For production deployment, see the comments below for required changes.
#
# Quick Start (Local Development):
#   1. Copy this file: cp .env.example .env
#   2. Generate a session secret: openssl rand -base64 48
#   3. Replace JUNJO_SESSION_SECRET value below with generated secret
#   4. Run: docker compose up -d
#   5. Access: http://localhost:5153
# ========================================================================================

# === Environment Mode ===================================================================
# Junjo Server's Running Environment:
# Value: development || production
#
# - development: Uses localhost and ports for services (default for Quick Start)
# - production:  Uses JUNJO_PROD_AUTH_DOMAIN with subdomains (requires reverse proxy)
JUNJO_ENV="development"

# === Production Settings (Only used when JUNJO_ENV="production") =======================
# Production Authentication Domain:
# Set this to your production top-level domain for session cookie authentication.
# This automatically covers all subdomains.
#
# Example: JUNJO_PROD_AUTH_DOMAIN="junjo.example.com"
# Covers: junjo.example.com, api.junjo.example.com, grpc.junjo.example.com
#
# IMPORTANT: Only needed for production deployment with a reverse proxy
JUNJO_PROD_AUTH_DOMAIN="example.com"

# === Session Secret =====================================================================
# Session Secret for authentication management
#
# ⚠️ REQUIRED: Generate a secure key with: openssl rand -base64 48
# Replace "your_secret_key" with the generated value
JUNJO_SESSION_SECRET="your_secret_key"

# === CORS Configuration =================================================================
# Allowed Origins for Cross-Origin Resource Sharing
# Comma-separated list of allowed origins
#
# Default (development): Allows localhost ports
# Production: Change to your actual domain(s)
#
# If not set, all origins are allowed (*) - NOT recommended for production
# Example: JUNJO_ALLOW_ORIGINS=http://localhost:5151,http://localhost:5153,http://example.com,https://example.com
JUNJO_ALLOW_ORIGINS=http://localhost:5151,http://localhost:5153